# ============================================================
# requirements2.txt — Clean, Ubuntu-compatible requirements
# Generated by auditing every import in the codebase.
# Only packages actually used (directly or as transitive deps)
# are listed here. torch/torchaudio/torchvision and pywin32
# have been intentionally excluded (see notes below).
# ============================================================

# -- Web Framework
fastapi==0.137.2
starlette==1.3.1
uvicorn==0.40.0
httptools==0.7.1
watchfiles==1.1.1
websockets==15.0.1

# -- Pydantic / Validation
pydantic==2.12.5
pydantic_core==2.41.5
pydantic-settings==2.12.0
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2

# -- Environment / Config
python-dotenv==1.2.1

# -- Supabase (DB + Auth)
supabase==2.31.0
supabase-auth==2.31.0
supabase-functions==2.31.0
storage3==2.31.0
realtime==2.31.0
postgrest==2.31.0
httpx==0.28.1
httpx-sse==0.4.3

# -- Mistral AI SDK
mistralai==2.5.0
eval_type_backport==0.3.1
python-dateutil==2.9.0.post0
jsonpath-python==1.1.6
opentelemetry-api==1.39.1
opentelemetry-semantic-conventions==0.60b1

# -- Cloudinary (image/snapshot upload)
cloudinary==1.44.2

# -- Playwright (headless browser snapshots)
playwright==1.57.0
pyee==13.0.0
greenlet==3.3.0

# -- ML / Clustering (scikit-learn + its deps)
# NOTE: torch is NOT required by scikit-learn, numpy, or scipy
scikit-learn==1.8.0
numpy==2.2.6
scipy==1.16.3
joblib==1.5.3
threadpoolctl==3.6.0

# -- HTTP / Networking
aiohttp==3.13.3
aiohappyeyeballs==2.6.1
aiosignal==1.4.0
frozenlist==1.8.0
multidict==6.7.0
yarl==1.22.0
propcache==0.4.1
attrs==25.4.0
requests==2.32.5

# -- Cryptography (API key encryption in security/encryption.py)
cryptography==46.0.3
cffi==2.0.0

# -- Markdown processing (md_to_html.py)
Markdown==3.10

# -- General utilities (transitive deps of the above)
anyio==4.12.0
sniffio==1.3.1
certifi==2025.11.12
charset-normalizer==3.4.4
idna==3.11
urllib3==2.3.0
h11==0.16.0
click==8.3.1
six==1.17.0
packaging==25.0
annotated-doc==0.0.4

